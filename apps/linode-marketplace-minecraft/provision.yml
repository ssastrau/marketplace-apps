---
# linode ansible playbook

# linodes
- name: Setting up environment
  hosts: localhost
  connection: local
  any_errors_fatal: true
  user: root
  vars_files: [group_vars/linode/vars]
  tasks:
    - name: Generating sudo user and password
      import_tasks: ../linode_helpers/roles/sudouser/tasks/main.yml

    - name: Generating linuxgsm user password
      lineinfile:
        insertafter: EOF
        path: group_vars/linode/vars
        line: |
          linuxgsm_password: {{ lookup('password', '/dev/null length=45 chars=ascii_letters,digits') }}

    - name: Generating rcon password
      lineinfile:
        insertafter: EOF
        path: group_vars/linode/vars
        line: |
          rconpassword: {{ lookup('password', '/dev/null length=25 chars=ascii_letters,digits') }}
